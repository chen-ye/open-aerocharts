{
  "name": "avmvt-workspace",
  "private": true,
  "workspaces": [
    "client"
  ],
  "scripts": {
    "dev": "npm run dev -w client",
    "build": "npm run build -w client",
    "lint": "npm run lint -w client && cd faa-ais-pmtiles && uv run ruff check src",
    "preview": "npm run preview -w client"
  },
  "devDependencies": {
    "@types/react-map-gl": "^6.1.8",
    "ts-morph": "^28.0.0"
  }
}
