name: Update FAA PMTiles

on:
  schedule:
    # FAA updates run on a 28 day cycle, typically effective on Thursdays.
    # Running weekly on Thursdays at 10 AM UTC ensures we catch the new cycle when it drops.
    - cron: '0 10 * * 4'
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  update-pmtiles:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          lfs: true

      - name: Install uv
        uses: astral-sh/setup-uv@v5

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version-file: "faa-ais-pmtiles/pyproject.toml"

      - name: Run datagen pipeline
        run: |
          cd faa-ais-pmtiles
          uv run build-pmtiles

      - name: Create Pull Request
        uses: peter-evans/create-pull-request@v6
        with:
          commit-message: "chore(data): update FAA PMTiles"
          title: "Automated FAA Data Cycle Update"
          body: |
            Automated pull request to update FAA aeronautical datasets to the latest 28-day cycle.

            The data pipeline has fetched the latest CIFP and generated new `.pmtiles`.
          branch: "chore/update-faa-data"
          base: "main"
          delete-branch: true
